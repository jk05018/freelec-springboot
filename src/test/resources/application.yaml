spring:
  jpa:
    show-sql: true

  h2:
    console:
      enabled: true
  session:
    store-type: jdbc
#  datasource:
#    driver-class-name: org.h2.Driver
#    url: jdbc:h2:mem:testdb
#    username: sa
#    password:
  # test oauth
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test
            client-secret: test
            scope: profile, email
